[pytest]
addopts = -q --cov=. --cov-fail-under=70 --cov-report=term-missing
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    gpu: requires CUDA/GPU (deselect with '-m "not gpu"')
