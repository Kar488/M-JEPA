# Core
numpy
pandas
pyarrow                 # parquet I/O
scikit-learn
torch==2.2.1         # CUDA wheels handled separately in CI
torch-scatter==2.1.2 # optional, used in gnn_variants.py; CUDA wheels handled separately in CI
       
#rdkit-pypi              # or install via conda:  conda install -c conda-forge rdkit
pyyaml
joblib
fastparquet

# Logging / plotting
wandb
matplotlib
seaborn

# Data utilities
deepchem                # optional, only for MoleculeNet downloads

# Testing
pytest
pytest-cov
