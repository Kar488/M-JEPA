# Core
numpy<2
pandas
pyarrow                 # optional parquet I/O (install pyarrow or fastparquet)
scikit-learn
#torch==2.2.1         # done through git workflow but if we want clean local test environments (perhaps move later to environment.yml)
#torch-scatter==2.1.2 # done through git workflow but if we want clean local test environments (perhaps move later to environment.yml)
#torch-geometric==2.5.3 # done through git workflow but if we want clean local test environments (perhaps move later to environment.yml)
tensorflow

# RDKit core (already included via conda in prepare_env.sh)
# Latest stable RDKit wheels on PyPI stop at 2022.9.x; wheels are unavailable for Python>=3.12.
rdkit-pypi==2022.9.5; python_version < "3.12"  # PyPI-friendly RDKit builds for graph visuals and featurisation
# Optional RDKit drawing dependencies for 2-D/3-D rendering (see scripts/ci/generate_graph_visuals.py)
pillow
pyyaml
joblib
fastparquet             # optional parquet I/O alternative

# Logging / plotting
wandb
wandb-workspaces        # optional, needed for W&B Reports API
matplotlib
seaborn
buildamol             # optional, used for interactive 3-D graph visuals
gymnasium             # drop-in replacement for gym (required by buildamol)

# Data utilities
#deepchem                # optional, only for MoleculeNet downloads, # done through git workflow
tqdm
psutil

# Testing
pytest
pytest-cov
