# Core
numpy<2
pandas
pyarrow                 # parquet I/O
scikit-learn
torch==2.2.1         # CUDA wheels handled separately in CI
#torch-scatter==2.1.2 # optional, used in gnn_variants.py; CUDA wheels handled separately in CI removed – install explicitly after torch
torch-geometric==2.5.3
       
#rdkit-pypi              # or install via conda:  conda install -c conda-forge rdkit
pyyaml
joblib
fastparquet

# Logging / plotting
wandb
matplotlib
seaborn

# Data utilities
deepchem                # optional, only for MoleculeNet downloads

# Testing
pytest
pytest-cov
