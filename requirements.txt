# Core
numpy<2
pandas
pyarrow                 # parquet I/O
scikit-learn
#torch==2.2.1         # done through git workflow
#torch-scatter==2.1.2 # done through git workflow
#torch-geometric==2.5.3 # done through git workflow
tensorflow
       
#rdkit-pypi              # # done through git workflow or install via conda:  conda install -c conda-forge rdkit
pyyaml
joblib
fastparquet

# Logging / plotting
wandb
matplotlib
seaborn

# Data utilities
#deepchem                # optional, only for MoleculeNet downloads, # done through git workflow
tqdm

# Testing
pytest
pytest-cov
