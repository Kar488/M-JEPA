# Core
numpy
pandas
pyarrow                 # parquet I/O
scikit-learn
torch ; sys_platform == "never"  # placeholder to avoid install on server
torch-scatter ; sys_platform == "never" # optional, used in gnn_variants.py
       
rdkit-pypi              # or install via conda:  conda install -c conda-forge rdkit
pyyaml
joblib
fastparquet

# Logging / plotting
wandb
matplotlib
seaborn

# Data utilities
deepchem                # optional, only for MoleculeNet downloads

# Testing
pytest
pytest-cov
